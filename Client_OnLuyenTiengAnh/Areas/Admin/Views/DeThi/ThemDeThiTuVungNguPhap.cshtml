@using Client_OnLuyenTiengAnh.Areas.Admin.Models
@model List<CauHoi>
@{
    ViewBag.Title = "ThemDeThiTuVungNguPhap";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    string maDe = ViewBag.maDe;
}

<h2 class="center">Thêm đề thi phần từ vựng ngữ pháp</h2>

<div class="row">
    <div class="col-xs-12">
        @using (Html.BeginForm("ThemDeThiNguPhapTuVung", "DeThi", FormMethod.Post))
        {
            <div class="center">
                <label>Mã đề: </label><input type="text" value="@maDe" disabled="disabled" />
            </div>
            <br />
            <br />

            <table id="simple-table" class="table  table-bordered table-hover">
                <thead>
                    <tr>
                        <th class="center">

                        </th>
                        @*<th class="detail-col">Details</th>*@
                        <th>Tiêu đề câu hỏi</th>
                        <th>Đán án A</th>
                        <th>Đáp án B</th>
                        <th>Đáp án C</th>
                        <th>Đáp án D</th>
                        <th>Đáp án</th>


                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="center">
                                <label class="pos-rel">
                                    <input type="checkbox" class="ace" name="maCH" value="@item.ID" />
                                    <span class="lbl"></span>
                                </label>
                            </td>

                            <td>@item.TieuDe</td>
                            <td>@item.PhuongAnA</td>
                            <td>@item.PhuongAnB</td>
                            <td>@item.PhuongAnC</td>
                            <td>@item.PhuongAnD</td>
                            <td>@item.DapAn</td>

                        </tr>
                    }







                </tbody>
            </table>
            <button type="button" class="btn btn-danger btn-sm">
                <span class="ace-icon fa fa-pencil-square-o icon-on-right bigger-110"></span>
                Hủy
            </button>
            <button type="submit" class="btn btn-success btn-sm">
                <span class="ace-icon fa fa-pencil-square-o icon-on-right bigger-110"></span>
                Tạo đề thi
            </button>
        }
    </div><!-- /.span -->
</div><!-- /.row -->
